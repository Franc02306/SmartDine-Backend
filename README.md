# SmartDine Backend

Proyecto backend inicial construido con **Node.js** y **Express**, pensado como base para una API REST simple.

## 📦 Inicialización del proyecto

```bash
mkdir smartdine-backend
cd smartdine-backend
code .
npm init -y


🧩 Instalación de dependencias principales

npm install express
npm install socket.io
npm install pg


▶️ Ejecutar el proyecto

node index.js



